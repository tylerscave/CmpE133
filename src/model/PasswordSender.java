package model;

/**
 * Used to send a password via email in the event that it is forgotten.
 * @author David Lerner
 */
public class PasswordSender {

    /**
     * send a password via email.
     * @param email email address of the member who needs the password
     */
    public void sendPassword(String email) {
        //get password from database and send it via email
        //stub
    }
}
